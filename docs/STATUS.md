# EventPulse status

Part 1 Stage 1 exact-state and isolation audit: **IN PROGRESS**.

The default branch contains the OIDC identity workflow. The streaming foundation is in [draft PR #1](https://github.com/bhuvaneshwaranmurugan21/eventpulse-realtime-engagement-platform/pull/1). Its local and infrastructure CI passed at that draft head. Private evidence verifies EventPulse OIDC role trust and bounded empty resource queries in `ap-south-1`. GitHub Actions now selects `ap-south-2`, while draft Terraform defaults to `ap-south-1`; the Hyderabad AWS scope, full resource inventory and cost remain unresolved. See [the Stage 1 audit](audits/part1-stage1-exact-state.md) for exact source, evidence, findings and limits.
